// utils/destiny/tone-adjust.js
// Small helper to adjust tone/labels in explanation output
function adjustTone(text, level = 'neutral') {
  // very small placeholder implementation
  return `[${level}] ${text}`;
}
module.exports = { adjustTone };
